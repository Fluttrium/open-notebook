[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.single"

[deploy]
healthcheckPath = "/api/config"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
startCommand = "/app/scripts/railway-entrypoint.sh"
